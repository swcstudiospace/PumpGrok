# Hermes runtime for PumpGrok

Isolated profiles + a file-bus cron state machine. Fits a headless VPS. Hermes Desktop is a laptop client pointed at that VPS over SSH. It does not run on the server.

See scripts/hermes-bootstrap.sh and scripts/hermes-install-cron.sh.
